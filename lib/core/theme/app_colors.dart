import 'dart:ui';

class AppColors {
  static const primary = Color(0xFF2A480A);

  static const secondary = Color(0xFF48470A);

  static const black = Color(0xFF0D1603);
  static const white = Color(0xFFDCF6C0);
  static const grey = Color(0xFFAFEB6F);
  static const bg = Color(0xFFF3FCE9);

  static const accent = Color(0xFF470A48);
  static const succe = Color(0xFF0B480A);
  static const danger = Color(0xFF480A0B);
  static const warning = Color(0xFFB8B519);
  static const info = Color(0xFF0A0B48);
}

// import 'package:flutter/material.dart';

// class AppColors {
//   const AppColors._();

//   // ─── PRIMARY (gestion / identité) ─────────────────────────────
//   static const primary = Color(0xFF2A480A); // ton vert
//   static const primaryLight = Color(0xFF5C8F1B);

//   // ─── TAXI (IMPORTANT UX) ─────────────────────────────────────
//   static const taxiYellow = Color(0xFFFFC107);
//   static const taxiYellowDark = Color(0xFFFFB300);
//   static const taxiYellowLight = Color(0xFFFFF8E1);

//   static const accent = Color(0xFF8A2A0A); // complémentaire
//   static const accentLight = Color(0xFFFFE5E0);

//   static const success = Color(0xFF0B480A); // dispo
//   static const successLight = Color(0xFFE8F5E9);

//   static const warning = Color(0xFF48470A); // maintenance
//   static const warningLight = Color(0xFFFFF3E0);

//   static const danger = Color(0xFFF44336); // panne
//   static const dangerLight = Color(0xFFFFEBEE);

//   static const info = Color(0xFF0A0B48); // course en cours
//   static const infoLight = Color(0xFFE3F2FD);

//   // ─── UI ─────────────────────────────────────────────────────
//   static const bg = Color(0xFFF3FCE9);
//   static const cardBg = Colors.white;

//   static const textPrimary = Color(0xFF111827);
//   static const textSecondary = Color(0xFF6B7280);

//   static const border = Color(0xFFE5E7EB);

//   static const shadow = Color(0x14000000);
//   static const shadowSoft = Color(0x0F000000);
// }
